# =====================================================================
# SaydiTool — Official Production Dependencies
# Pipeline: Crawler -> VAD Slicer -> 2-Pass MSS (Demucs -> Mel-Band RoFormer) -> ASR
# =====================================================================

# --- Media Downloader & Browser Impersonation ---
yt-dlp[curl-cffi,default]>=2026.7.4
curl-cffi>=0.15.0
requests>=2.31.0

# --- Core Audio DSP & Signal Processing ---
librosa>=0.10.0
soundfile>=0.12.1
scipy>=1.11.0
numpy>=1.24.0,<2.0.0
pyyaml>=6.0.1

# --- PyTorch Core ---
torch>=2.0.0
torchaudio>=2.0.0

# --- Official 2-Pass Separation Engines ---
# Tầng 1: Demucs AI (Meta AI) — Triệt tiêu Bass & Trống
demucs>=4.0.0

# Tầng 2: Mel-Band RoFormer (Rotary Transformer SOTA) — Khử sạch tàn dư dải cao
audio-separator>=0.47.0

# --- Audio Slicing & Voice Activity Detection ---
silero-vad>=4.0.0

# --- Speech Recognition & Evaluation ---
faster-whisper>=1.0.0

# --- Utilities ---
tqdm>=4.66.0
